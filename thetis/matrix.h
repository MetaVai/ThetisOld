#pragma once

/*
 * simple C++ interfaces are also possible apparently, using ~ the same mechanism. Please cf. swift examples in swift code 
 * for my interests (numerical computing) this might or might not be that important...
 */
int matrixFn0(const char *x);
